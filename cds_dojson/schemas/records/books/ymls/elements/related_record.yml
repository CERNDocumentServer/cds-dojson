additionalProperties: false
description: |-
  :MARC: ``775``
  .. Note:: in the current MARC model, we use 775__b and 775__c to indicate the year, edition and language.
properties:
  curated_relation:
    type: boolean
  note:
    description: Detailed description of the relation
    minLength: 1
    type: string
  record:
    $ref: json_reference.json
    description: ':MARC: ``775__w``'
    title: URL of related record
  relation:
    description: |-
      :MARC: depending on ``775__b`` and ``775__c``
      The possible values are:
      ``other edition``
      ``other language``
      ``parent``
      If none of the relations apply, a free textual relation can be
      provided in :ref:`relation_freetext`.
      .. note::
         This should be used to link the different editions (date, language) of a same book, but also to link one volume to its parent record. In this case the display should be adapted.
    enum:
      - edition
      - language
      - parent
      - other
    minLength: 1
    title: Relation of the linked record to this one
    type: string
required:
  - record
  - relation
title: Decorated link to a related record
type: object
